ROOT: data/pipeline/scalability/scib-pipeline
py_env : scib
r_env:

unintegrated_metrics: false

FEATURE_SELECTION:
  hvg: 2000

SCALING:
  - unscaled

METHODS:
  unintegrated:
    output_type: embed
  # scvi:
  #   output_type: embed

DATA_SCENARIOS:
  HLCA:
    file: data/pipeline/load_data/download/HLCAv1_core.h5ad
    batch_key: dataset
    label_key: ann_finest_level
    organism: human
    assay: expression
